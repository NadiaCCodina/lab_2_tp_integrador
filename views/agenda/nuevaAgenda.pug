extends ../layoutOp

block contenido 
    h1 Seleccione la especialidad a buscar
    form(action=`/seleccionar/medicoespecialidad?clave_sucursal=${clave_sucursal}` method="get")
        select#especialidadInput.form-control(name='clave_especialidad')
            each especialidad in especialidades
                option(value=`${especialidad.clave_especialidad}`)= especialidad.nombre_especialidad
        input(type="hidden" name="clave_sucursal" value=clave_sucursal)
        input(type="submit" value="Seleccionar")
    if error
        .alert.alert-warning
          p #{error}
    if medicos_especialidad
        h1.text-center.mt-4 Lista de Medicos de la especialidad #{especialidad.nombre_especialidad}
        .container.mt-4
            table.table.table-bordered.table-hover
                thead.thead-light
                    tr 
                        th Nombre 
                        th Matricula
                tbody 
                    each medico in medicos_especialidad
                        tr  
                            td= medico.nombre_completo
                            td= medico.matricula 
                            td 
                                form(action="/agenda/crear/" + medico.matricula, method="post")
                                    
                                    input(type="hidden" name="clave_sucursal" value=clave_sucursal)
                                    button.btn.btn-danger(type="submit") Seleccionar
